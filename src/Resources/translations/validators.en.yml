babdev_sylius_product_samples:
    order:
        allowed_samples:
            max_per_order: '{1}Only 1 sample may be requested per order.|]1,Inf[Only {{ limit }} samples may be requested per order.'
